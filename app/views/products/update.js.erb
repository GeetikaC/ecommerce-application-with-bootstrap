<% if @product.errors.any? %>
  $('#error').html("<%= escape_javascript render 'error' %>");
<% else %>
  $('#error').hide();
  $('#newProduct').modal('hide');
  $.growl.notice({ message: "Successfully updated the product!" });
  window.location = '../<%= @product.id %>';
<% end %>